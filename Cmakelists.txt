include(./.env.cmake OPTIONAL RESULT_VARIABLE LOCAL_ENV)
message(STATUS "Local .env.cmae: ${LOCAL_ENV}")

cmake_minimum_required(VERSION 3.11.0)

set(NAME GearHead-Engine)

message(STATUS "using ${CMAKE_GENERATOR}")
